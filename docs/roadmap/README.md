# SubversionR Roadmap

SubversionR is developed in the public repository with milestone history, release claims, and evidence gates kept reviewable alongside the implementation.

## Current Engineering Focus

After the Windows `win32-x64` Beta release and Marketplace pre-release publication, the main implementation focus is semantic hardening, installed VSIX negative/failure-flow breadth for implemented local workflows, and claim/evidence alignment. Signing/provenance, previous-stable rollback, final legal and vulnerability approval, broader accessibility, remote/auth breadth, cross-platform packages, and merge workflows remain later gates.

The `0.2.4` Windows candidate shipped on 2026-07-15 JST (2026-07-14 UTC) as GitHub pre-release [`v0.2.4-beta.1`](https://github.com/Hitsuki-Ban/SubversionR/releases/tag/v0.2.4-beta.1) and as Marketplace pre-release `hitsuki-ban.subversionr` with display name `SVN-R`. Its exact VSIX is covered by live attestation [`35359353`](https://github.com/Hitsuki-Ban/SubversionR/attestations/35359353), successful Marketplace publish run [`29376825168`](https://github.com/Hitsuki-Ban/SubversionR/actions/runs/29376825168), and the public Gallery state recorded in [`docs/release/0.2.4-publication-evidence.md`](../release/0.2.4-publication-evidence.md). Artifact signing, signed source-to-binary provenance, public-install verification, previous-stable rollback, final legal and vulnerability approval, and public readiness remain separate open gates.

## Windows Beta Closure

The Windows `win32-x64` Beta local workflow scope now covers checkout/open, status, add/remove/move/revert/resolve/update/commit/delete-unversioned, update-to-revision/depth/externals policy, properties and `svn:ignore`, changelists, lock/unlock, branch/tag create, and switch through the TypeScript adapter, Rust sidecar, and native `libsvn` bridge. Marketplace pre-release installation is available for this bounded Beta. Merge, merge preview, mergeinfo, stable-channel publication, signing/provenance publication, previous-stable rollback, cross-platform packages, broad remote/auth matrices, and coverage-guided native remote-protocol fuzzing remain outside the Beta claim.

## Windows Beta Packaging Readiness

The Beta package can be considered for candidate distribution only after the implemented local workflows have installed VS Code/VSIX evidence, the state engine has a minimum Windows-local performance gate, and the candidate VSIX plus release evidence are verified as a single same-run bundle. The packaging-readiness track is intentionally narrower than public release readiness:

- Installed E2E first: local-file Checkout SVN Repository… prompt cancellation, pre-existing obstructing target file failure/no-state-pollution flow, invalid URL failure/no-state-pollution flow, pre-existing local directory target success path, pre-existing local directory obstruction tree-conflict projection path, happy path, Update to Revision happy path plus revision prompt cancellation/no-state-pollution flow, Add to Ignore/`svn:ignore`, deterministic read-only repository/resource property-report tab reuse, changelist set/clear/commit/revert, Lock/Unlock/`svn:needs-lock`, Branch/Tag create, and Switch happy paths are covered; next installed slices should cover remaining checkout remote/auth/browser and broader failure flows, update remote/backend-failure/load edges, branch/switch target-browsing and edge/load behavior, and remaining failure/cancellation behavior.
- State-engine floor: Beta-F records `pnpm release:test-state-engine-beta-performance:win32-x64` evidence that single-file saves must not trigger full scans, event bursts must stay bounded, nested working copies and externals must remain isolated, dirty-generation supersede must not publish stale results, sidecar restart must recover or mark stale, and a 10k local working copy must have a recorded baseline. Native watcher production, adaptive cost feedback, 100k/1M scale, idle CPU measurement, and default background remote polling remain outside this floor.
- Candidate bundle consistency: Beta-G records `pnpm release:verify-beta-candidate:win32-x64` evidence that the current VSIX bytes, VSIX package evidence, installed VSIX evidence, native artifact map, provenance, publication gaps, state-engine floor, artifact bundle manifest, and explicit CI upload allowlist all belong to the same candidate bundle before CI uploads `subversionr-win32-x64-beta-candidate` with `actions/upload-artifact@v7`. The manual preparation entrypoint is `pnpm release:prepare-beta-candidate:win32-x64`, which regenerates the default candidate evidence paths in dependency order, runs `pnpm release:generate-beta-artifact-bundle-manifest:win32-x64`, and then runs the final consistency verifier.
- CI discipline: `.github/workflows/pr-fast.yml` is the automatic Windows pull-request gate; heavy native, packaging, installed-product, advisory, and publication gates remain scheduled/manual.
- Claim discipline: Beta may claim Marketplace pre-release distribution and local file-backed Windows `win32-x64` workflows only. Stable-channel publication, signing/provenance, previous-stable rollback, broad remote/auth, cross-platform, coverage-guided fuzzing, merge, merge preview, and mergeinfo remain outside the Beta package claim.

## Milestones

- M0: repository, environment, CI, and documentation baseline.
- M1: Apache Subversion 1.14.5 source gate, narrow C ABI, Rust sidecar, TypeScript extension, and protocol smoke.
- M2: repository discovery/open/close and minimal status snapshot.
- M3: dirty-path status engine with generation, epoch, targeted status, full reconcile, backend restart/heartbeat degradation handling, global degraded-backend context/status-bar surfacing, client-side status refresh cancellation/stale handling, and daemon/native status plus operation cancellation execution.
- M4: core SCM operations. Implemented slices currently cover libsvn-backed revert, add, remove, repository cleanup, conflict resolve, root update, repository-level Update Working Copy, selected incoming-resource update, file-target Commit Selected including same-repository SCM multi-selection, repository-level Commit All through Source Control input accept, explicit localized missing-message prompting with post-prompt selection revalidation, Review & Commit state retention until confirmed success, per-repository operation scheduling in the VS Code extension, a bounded sanitized operation journal included in diagnostics bundles, cancellable VS Code operation progress, and protocol v1.29 actionable failure reporting with a bounded redacted SubversionR log and preserved failed-commit form state.
- M5: content, diff, history, blame, and SVN Lens. Implemented slices currently cover the libsvn-backed `content/get` foundation for BASE, HEAD, and explicit revisions, Source Control QuickDiff BASE originals, explicit SCM resource commands for BASE/HEAD content and diff, backend-only `history/log`, backend-only `history/blame`, a native SCM `SVN History` TreeView for repository/file history, readonly explicit revision content from file history revision rows, foreground comparison of a file-history revision against the previous already loaded file-history revision, readonly Revision Details documents from already loaded history metadata, readonly BASE blame documents for local versioned SCM file resources, history clipboard commands, a projection-backed File Header CodeLens foundation for editor-visible history actions, CodeLens BASE/HEAD/PREV compare actions for projected files, projection-backed editor context menu entries for projected local SVN files, projection-backed Compare PREV from editor or SCM resources, loaded same-file history multi-select Compare Revisions, loaded-history row filtering, explicit settings for current-line, hover, and symbol SVN Lens surfaces, a conservative current-line blame status bar for projected text-stable SVN files, a matching current-line blame hover with first-line log summaries, an optional symbol history CodeLens baseline for projected text-stable SVN files, a conservative active-editor Line History command backed by preloaded libsvn blame/log rows, and exact active-editor Command Palette access to the six canonical BASE/HEAD/PREV/File History/Line History/Blame commands with installed trusted and Restricted Mode evidence.
- M6: auth, certificate, workspace trust, security diagnostics. Implemented slices currently cover a VS Code Workspace Trust limited-mode manifest declaration, packaged backend resource resolution for the Windows x64 host target without user-provided backend paths, `isWorkspaceTrusted` menu gating for current write/update/remote-read operations, runtime trust enforcement for cleanup/update/revert/add/remove/resolve/commit/HEAD/PREV/history/blame/line-history paths, virtual-document trust gates for HEAD/revision/blame content, Lens/hover trust suppression for blame/log calls, a stable `SUBVERSIONR_WORKSPACE_UNTRUSTED_OPERATION` error code, English/Japanese/Chinese trust-description localization, protocol/daemon `diagnostics/get`, `diagnosticsGet` capability under protocol v1.13, redacted diagnostics bundle and version-report commands, recursive default diagnostics redaction, diagnostics localization, protocol v1.14 credential request/response contracts, protocol v1.15 certificate trust request/response contracts, inbound JSON-RPC request handling, a VS Code SecretStorage-backed credential controller that blocks untrusted workspaces and background prompts, a VS Code SecretStorage-backed certificate trust controller with explicit reject/trust-once/trust-permanent decisions, background no-prompt behavior, exact fingerprint reuse, changed-certificate rejection/re-prompt semantics, English/Japanese/Chinese certificate UI localization, an external-tool/TortoiseSVN trust-boundary foundation for future executable/config/tunnel settings without live `svnNative.*` aliases, protocol v1.16 daemon-side auth request broker capabilities for credential and certificate challenge round-trips, hardened stdio auth waiting for cancellation, malformed envelopes, EOF, and unrelated traffic while a single auth request is pending, native libsvn `SVN_AUTH_CRED_SIMPLE` plus SSL server-trust prompt callbacks routed through the daemon broker with SHA-256 DER certificate identity checks, bounded stdio auth transport handling for oversized frames, inbound request floods, and daemon-side timeout checkpoints, a bounded reader-pump stdio path that lets credential and certificate auth waits time out even when no inbound frame arrives, a source-built `svnserve` staging gate plus localhost `svn://` credential fixture foundation, broker-required native update over authenticated `svn://` working copies, broker-required native HEAD content over authenticated `svn://` working copies, broker-required native commit over authenticated `svn://` working copies without automatic commit retry, daemon-side stale credential/certificate response consumption after auth timeout, monotonic native auth request ids across libsvn callbacks, broker-required native history log/blame over authenticated `svn://` working copies, Apache HTTP Server/PCRE2 source locks, a source-built PCRE2 Windows x64 staging gate with raw MSVC and CMake consumer probes, a source-built Apache HTTP Server 2.4.68 Windows x64 DAV/SSL/deflate substrate stage with CMake cache binding to staged APR/PCRE2/OpenSSL/zlib, source-built `mod_dav_svn`/`mod_authz_svn` module compilation into a separate composite httpd/Subversion DAV runtime with a load-only httpd probe, a localhost HTTPS DAV fixture proving libsvn/ra_serf HEAD content plus update flows through the certificate broker against the staged runtime, and public-readiness security documentation for the threat model, support handling, and M6-to-M7 release security decision boundary.
- M7: TortoiseSVN adapter, migration, platform packaging, performance gates, and public release preparation. Implemented slices currently cover M7a release-readiness gates for `SECURITY.md`, release blockers, security evidence status, public claim boundaries, and CI verification without claiming public release completion, an M7b `win32-x64` staged VS Code package layout gate with backend resource manifest, hash verification, AMD64 PE checks, allowlisted private libsvn/APR/OpenSSL/iconv runtime checks, and SVN CLI exclusion, an M7c generated CycloneDX source SBOM and third-party NOTICE evidence gate that keeps final SBOM review, license publication, CVE review, signing, provenance, and VSIX release readiness blocked, an M7d cache schema and migration report foundation with protocol v1.20 `cacheSchema`, explicit extension cache root startup configuration, extension-owned delete-and-reconcile cache reset behavior, localized clear-cache/report commands, and release blockers retained for full install/upgrade/rollback evidence, an M7e optional TortoiseSVN read-only GUI handoff with Workspace Trust gating, explicit `TortoiseProc.exe` validation, structured `log`/`diff`/`revisiongraph`/`blame` args, `shell: false` process launch, and deferred public claims for write dialogs, Repository Browser, remote mapping, and installed-product E2E coverage, an M7f isolated release install/upgrade/rollback fixture that copies verified `win32-x64` staged package layouts into target-scoped VS Code-style extension directories, rolls back to a synthetic previous version, records `MIG-009`/`MIG-010`/`TST-024` evidence, and keeps previous-stable rollback, artifact signing/signed provenance, and installed core workflow E2E as release blockers, an M7g real `win32-x64` VSIX package and isolated VS Code CLI install gate that records `SEC-015`/`MIG-009`/`TST-024` evidence, an M7h installed VSIX Extension Host version-report command smoke gate that proves the installed extension loads from the isolated extensions root, executes `subversionr.diagnostics.versionReport`, emits a readonly `subversionr.versionReport`, and preserves a recursive `.svn` sentinel, an M7i installed VSIX core workflow E2E gate that opens a source-built local SVN working-copy fixture through the packaged sidecar/libsvn bridge under fixture-local `APPDATA/Subversion` runtime config isolation and proves SCM projection for modified and unversioned resources, an M7j1 installed VSIX Source Control surface gate that proves live VS Code `SourceControl` groups/resource states for the same source-built fixture while keeping Marketplace/public install, artifact signing/signed provenance, and previous-stable upgrade/rollback blocked, an M7j2a unsigned provenance and Marketplace metadata preflight gate that binds exact VSIX bytes, package evidence, backend manifest, source locks, lockfiles, SBOM, NOTICE, support metadata, and M7j2b evidence by SHA256 while keeping artifact signing/signed provenance, Marketplace/public install, previous-stable rollback, final legal/CVE review, and public readiness blocked, an M7j2b live post-release GitHub artifact attestation evidence gate that verifies the exact released VSIX subject against source-controlled custom-predicate schema, bundle, and verification-result bytes, pins signer/source refs and digests, records the public workflow run and attestation URL, and signs explicit original-build-provenance and artifact-signature non-claims, an M7j3 installed VSIX Source Control UI E2E gate that captures DOM, accessibility-tree, and nonblank screenshot PNG evidence for the installed local fixture while keeping public install, artifact signing/signed provenance, previous-stable rollback, remote/auth/certificate workflows, and broader accessibility evidence blocked, an M7k1 public support intake and redaction preflight gate that verifies structured GitHub issue forms, blank-issue blocking, `SECURITY.md` routing, a maintainer redaction checklist, and a public support redaction fixture while keeping public repository publication and Marketplace readiness blocked, an M7k2a Marketplace listing metadata and icon preflight that packages a PNG listing icon, verifies SVN-focused keywords, binds icon hash continuity through staged layout, VSIX package evidence, and provenance, and keeps public repository metadata, publisher authorization, publish auth, Marketplace/public install, artifact signing/signed provenance, and previous-stable rollback blocked, an M7k2b publication gaps and publish-auth contract preflight that binds VSIX/provenance/package evidence and the verified live GitHub attestation by SHA256, records public repository metadata from the extension manifest, records owner-attested Marketplace Contributor authorization and configured Microsoft Entra ID publish authentication without identity or credential values, records the consistent published Beta candidate ZIP, and keeps Marketplace publication/public install, artifact signing/signed provenance, and previous-stable rollback blocked, an M7k2c secretless Marketplace pre-release pipeline that verifies the exact release subject and GitHub attestation before Entra login, requires the Marketplace pre-release manifest property, and blocks the current ineligible `0.2.0` VSIX before login without claiming publication, an M7l1 vulnerability review input-contract preflight that binds the CycloneDX SBOM by SHA256, creates OSV querybatch contracts for versioned npm/Cargo PURLs, an M7l2a live OSV vulnerability review evidence gate that consumes the M7l1 preflight and records ordered OSV querybatch results plus vulnerability details for npm/Cargo PURLs, an M7l2b native advisory review evidence gate that binds native source locks, live OSV evidence, and official/project advisory source contracts for every native source-lock component, an M7l2c native advisory triage input-contract gate that expands native components plus live OSV findings into release-blocking triage/remediation/VEX input rows, an M7l2d vulnerability decision evidence gate that binds explicit decision records, an M7l2e vulnerability decision input terminal-progress gate that verifies source-lock/advisory-source/artifact-map constraints, records 11 terminal native decisions, and keeps 0 manual/blocking rows under investigation, an M7l2f manual native advisory terminal-review gate that verifies no-dedicated-index native review coverage, requires CVE or named security-finding mapping plus two approvals for terminal grants, records six current terminal grants including APR-iconv, an M7l2g Expat terminal CVE-2026-45186 decision gate that binds `native:expat@2.8.1` to a fixed VEX decision for only CVE-2026-45186, an M7l2h zlib terminal CVE-2026-22184 decision gate that binds `native:zlib@1.3.2` to a not_affected VEX decision for CVE-2026-22184 because SubversionR does not build or ship the vulnerable standalone `contrib/untgz` utility, an M7l2i APR terminal CVE decision gate that binds `native:apr@1.7.6` to a fixed VEX decision for CVE-2023-49582, CVE-2022-24963, CVE-2022-28331, and CVE-2021-35940, an M7l2j APR-util terminal CVE decision gate that binds `native:apr-util@1.6.3` to a fixed VEX decision for CVE-2022-25147 and CVE-2017-12618, an M7l2k Serf terminal CVE-2014-3504 decision gate that binds `native:serf@1.3.10` to a fixed VEX decision for CVE-2014-3504, an M7l2l APR-iconv named security finding decision gate that binds `native:apr-iconv@1.2.2` to a not_affected VEX decision for `APR-ICONV-1.2.2-NO-PUBLISHED-ADVISORY`, an M7l3 native artifact map preflight gate that explicitly maps every native source-lock component and every packaged `win32-x64` nativeDependency artifact while keeping reproducible builds, source-to-binary compilation proof, artifact signing/signed source-to-binary provenance, Marketplace/public install, previous-stable rollback, and public release readiness blocked, an M7l4 malicious input corpus preflight that maps deterministic malicious path, credentialed URL redaction, log rendering, JSON-RPC response-shape, SVN server-response, and XML/DTD categories to exact tests or explicit blockers, an M7l5 native malicious DAV/XML fixture that relocates a source-built working copy to a localhost DAV responder and proves malformed REPORT XML becomes a stable history-log failure without auth or certificate prompts, an M7l6 native malicious `svn://` server-response fixture that swaps a source-built authenticated `svnserve` working-copy fixture to a stateful malicious loopback responder and proves malformed log responses become stable history-log failures without auth or certificate prompts, an M7l7 native remote-protocol fuzz readiness contract that binds future `svn://` history/log fuzz target requirements, nightly/cargo-fuzz/MSVC sanitizer tooling, source-built libsvn sanitizer instrumentation, and required run/coverage evidence while keeping coverage-guided fuzzing, Marketplace/public install, and public release readiness blocked, an M7l8 native remote-protocol fuzz target source preflight that adds `fuzz/Cargo.toml`, `svn_server_response_history_log`, a SHA256-bound seed corpus manifest, and source-only release evidence while keeping cargo-fuzz build/run, seed execution, sanitizer coverage, libsvn edge growth, crash discovery, provider-complete fuzzing, and public release readiness blocked, and an M7l9 native remote-protocol fixed seed harness smoke that adds `fuzz/Cargo.lock`, builds the cargo-fuzz target, executes only the SHA256-bound malicious seed once, records `libsvnFfiReached=false`, and keeps coverage-guided fuzzing, sanitizer-instrumented libsvn coverage, libsvn edge growth, crash discovery, provider-complete fuzzing, and public release readiness blocked.
- M7j3 is now also the installed local-file Checkout SVN Repository… prompt cancellation, pre-existing obstructing target file failure/no-state-pollution flow, invalid URL failure/no-state-pollution flow, pre-existing local directory target success path, pre-existing local directory obstruction tree-conflict projection path, happy-path, Update to Revision happy-path plus revision prompt cancellation/no-state-pollution flow, Add to Ignore/`svn:ignore`, deterministic read-only repository/resource property-report tab reuse, active-editor Command Palette trusted/Restricted Mode behavior for the six canonical inspection commands, changelist set/clear/commit/revert, Lock/Unlock/`svn:needs-lock`, Branch/Tag create, Switch, and repository lifecycle evidence gate. Marketplace pre-release publication was completed later; the remaining public release blockers cover signing/provenance, previous-stable rollback, remaining checkout remote/auth/browser and broader failure flows, update remote/backend-failure/load edges, property/changelist/load/cancellation breadth, branch/switch target-browsing and edge/load behavior, broad remote/auth/certificate workflows, broad lock-server/policy/load behavior, switch-after-copy, merge workflows, and broader accessibility evidence.
- Beta-G adds the same-run Beta candidate consistency gate: `pnpm release:prepare-beta-candidate:win32-x64` regenerates the candidate evidence set, `pnpm release:generate-beta-artifact-bundle-manifest:win32-x64` records the current payload files, then `pnpm release:verify-beta-candidate:win32-x64` binds the exact current VSIX bytes to VSIX package evidence, installed VSIX evidence, native artifact map, provenance, publication gaps, the Beta-F state-engine floor, artifact bundle manifest, and explicit CI upload allowlist before CI uploads `subversionr-win32-x64-beta-candidate` with `actions/upload-artifact@v7`.
