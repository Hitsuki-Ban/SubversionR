# Support

For security vulnerabilities, follow `SECURITY.md`.

For other issues, include the SubversionR version, VS Code version, Windows version, repository transport type, and the action that failed.

Use sanitized diagnostics evidence only.

Do not include secrets, credentials, private repository URLs, cookies, certificate private keys, or sensitive working-copy data in public reports.
